name: Publish RB Solutions Theme to rb.hajagency.com

on:
   push:
      branches:
         - development

jobs:
   web-deploy:
      name: 🚀 Deploy RB Solutions Theme to rb.hajagency.com
      runs-on: ubuntu-latest
      steps:
         - name: 🚚 Get Latest Code
           uses: HAJ-Agency/checkout@v6.0.1
           
         - name: 📂 Sync Files
           uses: HAJ-Agency/web-deploy@v1
           with:
              source-path: ./
              target-server: shishi.oderland.com
              remote-user: hajagenc
              private-ssh-key: ${{ secrets.SSH_KEY }}
              destination-path: /home/hajagenc/rb.hajagency.com/wp-content/themes/rb-solutions/
              rsync-options: --archive --verbose --compress --delete-after --human-readable --exclude=.git* --exclude=.git/ --exclude=README.md --exclude=readme.md --exclude=.gitignore

         - name: ✅ Done
           run: echo "Deployment to rb.hajagency.com complete!"
# https://github.com/SamKirkland/web-deploy
# https://github.com/actions/checkout
# https://stackoverflow.com/questions/58347746/automating-the-build-and-publish-process-with-github-actions-and-github-package
# https://github.com/actions/setup-node
